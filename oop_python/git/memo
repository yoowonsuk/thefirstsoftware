mercurial SCM
Apache Subversion

git-scm.com
binary installer
MAC OS: open with installer

pycharm -> VCS -> Enable Version Control Integration

shift + command + . => reveal file in MAC OS

.gitignore

pycharm menu -> preferences -> github
VSC menu -> Get from version control (clone) then pycharm menu -> preferences -> project -> python interpreter -> add -> OK



IMPORTANT!!
# first, install "translate" package in python interpreter
import translate import Translator

translator = Translator(to_lang='pt')
for g in greetings: # greetings is list
    print(translator.translate(g).title())

make file "requirements.txt" => 
for example) translate~=3.5.0

# console
pip install -r requirements.txt
