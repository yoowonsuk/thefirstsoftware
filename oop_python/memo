Object-Oriented Programming
#Functional Programming
#Procedural Programming

IDE
- IDLE
- VS Code
- PyCharm
- Jupyter (https://colab.research.google.com/)

execute : ctrl + enter
next cell : shift + enter
# means heading size

# All language elements are in this code
a = 4 
b = 5 
c = [6, 7, 8]

d = "HI"
"Hello"

def area(x):
    return x**2

area(3)

inty = 5 
listy = [6, 7]

stringy = "Hi"

import folium # pip3 install folium
azores = folium.folium.Map(location = (38, -27), zoom_start = 6)

type(inty), type(listy), type(stringy), type(azores)

"hi".captialze()
> "HI"

folium.folium.Map(location = (38, -27), zoom_start = 6).save("output.html")
!ls
folium.__file__

# just tobe consistent with other programmers, give a break line there and then define and underscore.

from random import randint
randint(0,9)

thony
sudo apt update
sudo apt install python3.9
sudo apt install snapd
sudo snap install pycharm-community --classic

ctrl+D in pycharm => copy
ctrl / => comment

git mercurial subversion
alt+enter => add field to the class, kind of shortcut
lamp: Reformat file
ctrl shift a => plugins => live coding
refactor
repl.it => URL + ?embed=1
github filestack-python
